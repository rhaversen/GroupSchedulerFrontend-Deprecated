import { Caveat, Fredoka, Open_Sans } from 'next/font/google'

const caveat = Caveat({
    subsets: ['latin']
})

const fredoka = Fredoka({
    subsets: ['latin']
})

const openSans = Open_Sans({
    subsets: ['latin']
})

export { caveat, fredoka, openSans }
